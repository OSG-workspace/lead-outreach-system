# targeting/

One doc per campaign describing who it goes after and why, in prose.

`<campaign>.md` is the targeting thesis. `<campaign>.OVERRIDES.md` holds
deliberate exceptions to it, with the reason and the date, so a later run can
tell an intentional carve-out from a mistake.

Nothing here is parsed by a script — it is context for you and for the writers.
