# Voice (US) — canonical spec for the `gap-writer` / `lead-writer` agents

> Skeleton. Required only for `draft_mode=custom` campaigns. Template-mode
> campaigns (the default) read their copy from the fixture's `pitch.json`
> instead and never open this file.

## The offer (for YOUR understanding, not to paste)

[What we do, in one paragraph. The writer needs to understand it to spot a real
gap — but must never paste this text into an email.]

## The things you find per business (do this FIRST)

1. [The specific, checkable flaw you look for — e.g. "intake runs through a
   contact form with no calendar behind it"]
2. [The evidence — a URL or an exact phrase on their page proving it]
3. [The fill — what changes for them, in one sentence, no product name]

## Hard email rules

- Salutation exactly as the pipeline requires: `Hello Mr./Mrs. <Surname>,`
  or `Hello <First>,`. Never bare `Hello,`, never `<Business> team,`.
- [Max length]
- One CTA. One link. No attachments.
- No em dashes or en dashes.
- Banned words: [your list]
- Never state a price, a percentage, or a claim you cannot evidence.

## Compliance

Keep the copy honest; the sender appends the unsubscribe and postal address.

## Mini example (illustrative ONLY — never paste; write fresh per business)

[One example email showing the shape. Make it good — the writers imitate it.]
