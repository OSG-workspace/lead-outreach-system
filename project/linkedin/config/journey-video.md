<!--
THE VIDEO FOLLOW-UP — one fixed operator message, sent 3 days after the outreach
to anyone who has not replied.

Fill in the text below EXACTLY as David gives it. Do not paraphrase it, do not
"improve" it, and do not let a model rewrite it: this is his message, not a
generated one. `{name}` is the only variable — it is replaced with the person's
first name, and nothing else is substituted.

While this file holds no text (only this comment), journey_tick.js REFUSES to
queue the video step and prints "no video message configured" instead of sending
a stub. That is deliberate: an empty follow-up is worse than a late one.

Put the video link in the message text itself.
-->
