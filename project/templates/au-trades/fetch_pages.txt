# Stage 4 page-list override for au-trades (see fetch_html.py module docstring).
# This vertical is solo/small owner-operator trade businesses (plumbers,
# electricians, locksmiths, roofers). /team, /leadership and /people pages
# essentially never exist for this ICP — they cost the same fetch as
# home+contact for close to zero yield, on EVERY one of the 6 pages/domain
# the default list would otherwise try. Add "team|/team" back if a run shows
# real yield being missed (check leads-dropped.json / enrich-summary.json).
home|/
contact|/contact
about|/about
