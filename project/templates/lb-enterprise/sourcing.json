{
  "_comment": "map = this campaign's original OSM config, unchanged. places = added 2026-08-10 so a swept OSM ledger no longer means a zero-candidate fire. See templates/README.md 'Places sources' for the Table A / budget / ledger-scoping rules that govern every fixture's places block.",
  "max_per_run": 350,
  "sources": [
    {
      "type": "map",
      "targets": [
        {
          "selector": "\"office\"=\"company\"",
          "vertical": "enterprise"
        }
      ]
    },
    {
      "type": "places",
      "_note": "corporate_office IS Table A (Business). It carries no size or prominence facet, so it cannot express the 'biggest Lebanese companies' ICP by itself; the 150-request cap and the ICP gates downstream bound the noise.",
      "max_requests": 150,
      "targets": [
        {
          "included_type": "corporate_office",
          "vertical": "enterprise-lb"
        }
      ]
    }
  ]
}
